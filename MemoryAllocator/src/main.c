#include <stdio.h>
#include "sfmm.h"

int main(int argc, char const *argv[]) {
    // sf_malloc(32);
    // void *toFree = sf_malloc(100);
    // sf_malloc(32);
    // sf_free(toFree);
    // sf_show_heap();
    // sf_memalign(1, 128);
    // sf_show_heap();
    return EXIT_SUCCESS;
}

/*
 * Just a reminder: All non-main functions should
 * be in another file not named main.c
 */
